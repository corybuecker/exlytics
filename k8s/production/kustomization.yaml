resources:
  - ../base

namePrefix: production-

commonLabels:
  application: production-exlytics

patchesStrategicMerge:
  - |-
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: exlytics
    spec:
      template:
        spec:
          affinity:
            podAntiAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchExpressions:
                      - key: application
                        operator: In
                        values:
                          - production-exlytics
                  topologyKey: kubernetes.io/hostname
          containers:
            - name: nginx
              envFrom:
                - secretRef:
                    name: exlytics-production-ueqyziqqf
